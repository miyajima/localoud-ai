# Qwen3.8-27Bによる同一課題のローカル再実行

2026-09-10 JST。ユーザー指定の `http://127.0.0.1:18086/v1` を使用。**初回の生成結果が、変更なしで同じ受け入れテストに全合格した。Lunaは呼び出していない。**

| 指標 | 前回Spark-X初稿 | 今回Qwen初稿 |
|---|---:|---:|
| 入力トークン | 417 | 392 |
| 出力トークン | 1,752 | 1,163 |
| 合計ローカルトークン | 2,169 | 1,555 |
| 呼び出し経過時間 | 36.33秒 | 33.67秒 |
| 固定6テスト群 | 3群失敗 | 全合格 |
| 追加修正・再生成 | なし | なし |

モデル一覧・要求・応答のIDは `mtplx-qwen38-27b-optimized-speed`。このサーバーIDで実行し、checkpointや量子化の実体を別途検査したものではない。

前回のSpark-X要求JSONをコピーし、modelフィールドだけを変更した。契約・system指示・temperature=0・max_tokens=4096・chat_template_kwargs.enable_thinking=falseは同一。これら生成設定は要求値であり、各サーバー内部での適用は独立検証していない。モデルごとにトークナイザーが異なるため、トークン数の差をそのまま効率や課金額の差とは扱わない。

同じacceptance.pyで、境界値、型エラー、空集合、負数、大整数、入力不変性、および固定seedの250ケースを検証した。テストは生成後に親が実行し、その結果をモデルへ渡していない。親のソースレビューもpass。ローカル出力の修正は行っていない。

1課題・各1回の参考比較。今回の課題ではQwen単独で合格したが、Qwen→Lunaのトークン削減量を測ったものではない。前回Spark-Xとはサーバー実装・実行時点も異なる。時間はHTTP要求から応答読了までで、モデルのロード状況やウォームアップ状態は制御していない。

証拠: [要求](request.json)、[応答](response.json)、[使用量](metrics.json)、[無修正の実装](intervals.py)、[テスト結果](tests.txt)、[入力ハッシュ](frozen-inputs.json)、[実行スクリプト](../run_qwen.py)。既存のSpark-X/Luna結果は保持している。
