Implement intervals.py: normalize(intervals), subtract(intervals,cuts), contains(intervals,point). Read policy.md for the exact interval policy before implementing. Implement a robust interval-set subtraction, normalization and membership utility using Python standard library. Add your own unit tests in test_intervals.py, run them and acceptance.py. All 9 fixed acceptance tests and your own edge-case tests must pass. Only edit intervals.py and test_intervals.py. Do not modify acceptance.py, policy.md or AGENTS.md. No delegation, network access, or unrelated project access. Work only in the assigned fixture directory. Report the changed files and validation results concisely.
