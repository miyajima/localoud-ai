document.querySelector('#connect').onclick=async()=>{const token=document.querySelector('#token').value.trim();if(!/^[a-f0-9]{64}$/.test(token)){document.querySelector('#status').textContent='接続キーを確認してください。';return;}await chrome.storage.local.set({token});await chrome.tabs.create({url:'https://chatgpt.com/'});window.close();};document.querySelector('#inspect').onclick=async()=>{const [tab]=await chrome.tabs.query({active:true,currentWindow:true});try{const result=await chrome.tabs.sendMessage(tab.id,{kind:'inspect'});document.querySelector('#status').textContent=result.error||'モデルを確認しました。アプリのモデル一覧を更新してください。';}catch(e){document.querySelector('#status').textContent='ChatGPTのタブで開いてください。';}};